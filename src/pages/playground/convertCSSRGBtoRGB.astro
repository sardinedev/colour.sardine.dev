---
import PlaygroundLayout from "../../layouts/PlaygroundLayout.astro";
---

<PlaygroundLayout title="convertCSSRGBtoRGB">
  <script slot="script" is:inline type="module">
    import { convertCSSRGBtoRGB } from "https://unpkg.com/@sardine/colour@2.4.0/dist/index.min.js";

    function convertOnSubmit(rgb) {
      try {
        const res = convertCSSRGBtoRGB(rgb);
        document.getElementById("result").innerHTML = JSON.stringify(res);
        document.getElementById("result").style.backgroundColor = rgb;
      } catch (error) {
        document.getElementById("result").innerHTML = error;
      }
    }

    window.convertOnSubmit = convertOnSubmit;
  </script>

  <div slot="body">
    <label for="rgb">Type a colour in the CSS RGB format</label>
    <input
      type="text"
      id="rgb"
      class="input"
      placeholder="rgb(255, 255, 255)"
    />
    <button
      type="button"
      onclick="convertOnSubmit(document.getElementById('rgb').value)"
    >
      Convert
    </button>
    <label id="result"></label>
  </div>
</PlaygroundLayout>
